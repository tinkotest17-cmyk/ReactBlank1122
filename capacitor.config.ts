import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reactblank1122.app',
  appName: 'ReactBlank1122',
  webDir: 'dist/spa'
};

export default config;
